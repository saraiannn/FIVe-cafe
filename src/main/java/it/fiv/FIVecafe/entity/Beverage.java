package it.fiv.FIVecafe.entity;

public interface Beverage {  //(component) defining all methods that each base object and decorator must meet
     String getBeverageName();
     double getBeveragePrice();

}
